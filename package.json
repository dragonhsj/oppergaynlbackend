{
  "name": "oppergaynl-lastfm-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker proxy voor de Last.fm API",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250501.0",
    "typescript": "^5.4.5",
    "wrangler": "^3.60.0"
  }
}
